name: Cut a release

# One tap from the GitHub mobile app: Actions → Cut a release → Run workflow.
#
# It exists because a tag is the one step of a release that needed a laptop.
# This reads the version from main, creates `v<version>` there, and starts
# the Release workflow on that tag. Everything that makes the release public
# on a registry still waits in Release's `release` environment for the
# owner's approval — this only saves typing a git command.
#
# Two things GitHub does are load-bearing here:
#
#   - A tag pushed with GITHUB_TOKEN starts no workflow at all, deliberately,
#     so pushing it alone would produce a tag and nothing else. Release is
#     therefore dispatched explicitly — `workflow_dispatch` is the documented
#     exception to that rule.
#   - It is dispatched *on the tag*, so the run's ref is `refs/tags/v…`. The
#     `release` environment only admits tags matching `v*`; a run on `main`
#     would be refused before it could ask for approval.
on:
  workflow_dispatch:

# One at a time: two taps must not race to create the same tag.
concurrency:
  group: cut-release
  cancel-in-progress: false

jobs:
  cut:
    runs-on: ubuntu-latest
    permissions:
      contents: write # push the tag
      actions: write # dispatch Release
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Only from main
        run: |
          if [ "$GITHUB_REF" != "refs/heads/main" ]; then
            echo "::error::Releases are cut from main, and this run is on $GITHUB_REF."
            exit 1
          fi

      - name: Which version
        id: version
        run: |
          cargo_v=$(grep -m1 '^version = ' Cargo.toml | cut -d'"' -f2)
          npm_v=$(grep -m1 '"version"' npm/package.json | cut -d'"' -f4)
          if [ "$cargo_v" != "$npm_v" ]; then
            echo "::error::Cargo.toml says $cargo_v and npm/package.json says $npm_v. Bump both in a PR first."
            exit 1
          fi
          echo "tag=v$cargo_v" >> "$GITHUB_OUTPUT"
          echo "Cutting v$cargo_v from $(git rev-parse --short HEAD)"

      # A release of a commit whose CI did not pass is the one thing worth
      # refusing outright: nothing downstream re-runs the test suite.
      - name: CI passed on this commit
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          run_id=$(gh run list --workflow ci.yml --commit "$GITHUB_SHA" --event push \
            --json databaseId --jq '.[0].databaseId')
          if [ -z "$run_id" ]; then
            echo "::error::No CI run for $GITHUB_SHA yet. Wait for it, then run this again."
            exit 1
          fi
          # The whole run, not only the job named `ci`: the Windows jobs
          # test the client the release ships for Windows, so a red one
          # blocks a release as much as a red Linux one. On a push to main
          # every job runs; a run is `success` only when each of them is.
          read -r status conclusion < <(gh run view "$run_id" --json status,conclusion \
            --jq '"\(.status) \(.conclusion // "none")"')
          if [ "$status" != "completed" ] || [ "$conclusion" != "success" ]; then
            not_green=$(gh run view "$run_id" --json jobs --jq \
              '[.jobs[] | select(.conclusion != "success" and .conclusion != "skipped") | "\(.name): \(if (.conclusion // "") == "" then .status else .conclusion end)"] | join(", ")')
            echo "::error::CI on $GITHUB_SHA is '$status/$conclusion', not success (${not_green:-no job reported}). Wait for it, or fix it first."
            exit 1
          fi
          echo "CI: every job green (run $run_id)"

      # A tag that already exists means this version was released, or
      # started to be. Pointing here, it is an interrupted run and Release is
      # dispatched again — every publish step skips what is already done.
      # Pointing anywhere else, the version was not bumped: refuse, rather
      # than move a tag someone may already have installed from.
      - name: Create the tag
        id: tag
        env:
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          existing=$(git ls-remote --tags origin "refs/tags/$TAG^{}" | cut -f1)
          [ -n "$existing" ] || existing=$(git ls-remote --tags origin "refs/tags/$TAG" | cut -f1)
          if [ -n "$existing" ]; then
            if [ "$existing" = "$GITHUB_SHA" ]; then
              echo "::notice::$TAG already exists here — resuming, nothing already published is republished."
              exit 0
            fi
            echo "::error::$TAG already exists at ${existing:0:8}, not at ${GITHUB_SHA:0:8}. Bump the version in a PR first."
            exit 1
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "$TAG" -m "recall ${TAG#v}" "$GITHUB_SHA"
          git push origin "refs/tags/$TAG"

      - name: Start the Release workflow on the tag
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          gh workflow run release.yml --ref "$TAG" -f tag="$TAG"
          {
            echo "### $TAG is tagged and building"
            echo ""
            echo "The **Release** workflow is running on \`$TAG\`. In about ten minutes it"
            echo "asks for approval of the \`release\` environment — GitHub Mobile notifies"
            echo "you, and approving there publishes npm, crates.io and the Homebrew tap."
          } >> "$GITHUB_STEP_SUMMARY"
