{
  "name": "recall-server",
  "version": "0.1.0",
  "private": true,
  "description": "Recall sync server — POST/GET /sync backed by SQLite, no external dependencies.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
